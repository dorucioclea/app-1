import React, { PureComponent } from 'react';
import { FilesWidget } from 'dan-components';

class FilesStatus extends PureComponent {
  render() {
    return (
      <div>
        <FilesWidget />
      </div>
    );
  }
}

export default FilesStatus;

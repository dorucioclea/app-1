import React, { PureComponent } from 'react';
import { MapWidget } from 'dan-components';

class LocationStatus extends PureComponent {
  render() {
    return (
      <div>
        <MapWidget />
      </div>
    );
  }
}

export default LocationStatus;

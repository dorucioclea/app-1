import React, { PureComponent } from 'react';
import { ProfileWidget } from 'dan-components';

class NameStatus extends PureComponent {
  render() {
    return (
      <div>
        <ProfileWidget />
      </div>
    );
  }
}

export default NameStatus;

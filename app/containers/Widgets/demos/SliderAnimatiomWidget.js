import React, { PureComponent } from 'react';
import { SliderWidget } from 'dan-components';

class SliderAnimatiomWidget extends PureComponent {
  render() {
    return (
      <div>
        <SliderWidget />
      </div>
    );
  }
}

export default SliderAnimatiomWidget;
